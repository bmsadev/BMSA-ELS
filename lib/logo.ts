// BMSA Logo for email header
// Using a simple text-based logo to keep email size small
// (Gmail clips emails larger than 102KB, so base64 images are too large)

export const BMSA_LOGO_BASE64 = '';
